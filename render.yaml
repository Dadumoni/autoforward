services:
  - type: worker
    name: telegram-auto-forward-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python auto_forward_bot.py
    autoDeploy: false
